import { FilterJobApplicationsPipe } from './filter-job-applications.pipe';

describe('FilterJobApplicationsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterJobApplicationsPipe();
    expect(pipe).toBeTruthy();
  });
});
